"""Adapters (Step 12): `base.py` (protocolo AgentAdapter,
AdapterCapabilities), `claude_code.py` (ClaudeCodeAdapter, tradução de
hook pro evento comum). O script que de fato chama isso a partir de um
hook do Claude Code vive em `hooks/claude_code_hook.py`, na raiz do
repo, não aqui dentro do pacote.
"""
